const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const msg = await prisma.whatsAppMessage.findFirst({ where: { messageData: { path: ['imageMessage'], not: null } } }); const thumb = msg.messageData.imageMessage.jpegThumbnail; console.log('Type of thumb:', typeof thumb); if (thumb && typeof thumb === 'object') { console.log('Is Array?', Array.isArray(thumb)); console.log('Keys:', Object.keys(thumb)); if(thumb.type === 'Buffer') { console.log('It is a buffer json object'); } } else if (typeof thumb === 'string') { console.log('It is a string! Starts with:', thumb.substring(0,20)); } } main().finally(() => prisma.$disconnect());
