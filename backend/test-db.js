const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const msgs = await prisma.whatsAppMessage.findMany({ where: { remoteJid: { contains: '7081957371' } } }); console.log('WA Msgs:', msgs.length, msgs[0]); const lead = await prisma.lead.findFirst({ where: { phone: { contains: '7081957371' } } }); console.log('Lead ID:', lead?.id); } main().finally(() => prisma.$disconnect());
